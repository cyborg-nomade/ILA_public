AFTER DEPLOY
	- SYSTEMS ENDPOINTS
	- CLEANER CODE PRACTICES
	- TRY CLEAN ARCH
	- dismember form into section components
- at this point, create your own access, take back login method to normalcy, returns "isDeveloper"
	- your access shows
		- change DPO
			- this MUST be implemented before the below
		- DPO frontpage
		- comite frontpage (you can select from lateral menu)
		- user frontpage (you can type a user to get their groups) (USERS: GetUserGroups)
		- all cases
		- all access requests
		- all threads